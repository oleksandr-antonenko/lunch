# Lunch Project

pnpm monorepo: `apps/api` (NestJS), `apps/web` (Next.js), `packages/shared`, `packages/api-client`.

## Testing Rule

**Before finishing every interaction where code was written or modified**, run `/e2e-tests` to generate and execute e2e tests covering the fresh changes. Do not skip this step.
