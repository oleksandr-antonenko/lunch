{
  "name": "@lunch/api-client",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "openapi-generator-cli generate",
    "clean": "rm -rf src/generated",
    "build": "tsc"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.15.3",
    "typescript": "^5.7.3"
  }
}
